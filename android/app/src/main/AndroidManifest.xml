<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application android:name=".app.NodeScopeApplication" android:icon="@drawable/app_icon" android:dataExtractionRules="@xml/data_extraction_rules" android:label="@string/app_name" android:theme="@style/Theme.NodeScope" android:allowBackup="false" android:fullBackupContent="false" android:usesCleartextTraffic="false" android:supportsRtl="true">
        <!-- singleTop: a link opened while the app is running reaches onNewIntent instead of a second copy. -->
        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- nodescope://node|observer|channel|packet/<identifier>, the same links iOS shares. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nodescope" />
                <data android:host="node" />
                <data android:host="observer" />
                <data android:host="channel" />
                <data android:host="packet" />
            </intent-filter>
        </activity>
    </application>
</manifest>
